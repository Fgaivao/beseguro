

<%= form_with(model: seguro, local: true) do |form| %>
  <% if seguro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(seguro.errors.count, "error") %> prohibited this seguro from being saved:</h2>

      <ul>
      <% seguro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :descrição %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :imagem %>
    <%= form.file_field :image %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    <%= form.select :tipo, ['Particular', 'Empresa', 'Oferta', 'Negocio']%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

